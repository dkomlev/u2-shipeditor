# U2 — Фазовый план разработки для ИИ-агентов

Отличный план! Учитывая ваш опыт (пусть и 24-летней давности) и использование Copilot, вот пошаговый план разработки:

## 🎯 Фазовый план разработки для ИИ-агентов

### 📋 **Подготовительная фаза (1-2 дня)**

#### 1. Настройка среды разработки

```markdown
- Установить VS Code + Copilot
- Установить Git
- Настроить базовый HTML/JS окружение
- Создать локальный веб-сервер (Live Server extension)
```

#### 2. Восстановление базовых навыков

```markdown
- Освежить в памяти: HTML5, CSS3, современный JavaScript (ES6+)
- Изучить: Canvas API, модули ES6, async/await
- Понять разницу с 2000 годом: модули, промисы, классы
```

### 🏗️ **Фаза 1: Базовый каркас (3-5 дней)**

#### 1. Минимальная HTML структура

```html
<!-- index.html -->
<!DOCTYPE html>
<html>
<head>
    <title>U2 FlightTest</title>
    <style>/* базовые стили */</style>
</head>
<body>
    <canvas id="gameCanvas"></canvas>
    <script type="module" src="js/main.js"></script>
</body>
</html>
```

#### 2. Модульная структура JavaScript

```
js/
├── main.js          # точка входа
├── simulation/      # физика
├── rendering/       # отрисовка  
├── input/          # управление
├── config/         # загрузка конфигов
└── utils/          # утилиты
```

#### 3. Загрузка конфигураций

```javascript
// js/config/loader.js
export async function loadConfig(path) {
    const response = await fetch(path);
    return await response.json();
}
```

### 🎮 **Фаза 2: Базовая симуляция (1-2 недели)**

#### 1. Простая физика без релятивизма

```javascript
// js/simulation/basic-physics.js
class BasicPhysics {
    update(deltaTime) {
        // Пока без SR-эффектов
        this.position.x += this.velocity.x * deltaTime;
        this.position.y += this.velocity.y * deltaTime;
    }
}
```

#### 2. Рендеринг корабля-прямоугольника

```javascript
// js/rendering/simple-renderer.js
drawShip(ship) {
    ctx.fillStyle = 'white';
    ctx.fillRect(ship.x, ship.y, ship.width, ship.height);
}
```

#### 3. Базовое управление с клавиатуры

```javascript
// js/input/keyboard.js
handleKeyDown(event) {
    if (event.key === 'w') this.thrust = 1;
    if (event.key === 'a') this.rotation = -1;
    // ...
}
```

### ⚡ **Фаза 3: Добавление сложности (2-3 недели)**

#### 1. Релятивистские эффекты (постепенно)

```javascript
// js/simulation/relativistic-physics.js
applyRelativisticEffects() {
    const gamma = 1 / Math.sqrt(1 - (this.speed / C_PRIME) ** 2);
    // Постепенно добавляем /γ³ и /γ клампы
}
```

#### 2. Загрузка и применение ship.json

```javascript
// js/config/ship-loader.js
applyShipConfig(shipConfig) {
    this.mass = shipConfig.mass.mass_kg;
    this.thrust = shipConfig.propulsion.main_engine_thrust_max_N;
    // ...
}
```

#### 3. Система RCS и поворотов

```javascript
// js/simulation/rcs-system.js
applyRCSRotation(direction) {
    const torque = this.rcs.turn_alpha_max_radps2 * direction;
    this.angularAcceleration = torque / this.momentOfInertia;
}
```

### 🎛️ **Фаза 4: Интерфейс и управление (1-2 недели)**

#### 1. HUD с базовой информацией

```javascript
// js/rendering/hud.js
drawHUD() {
    ctx.fillText(`Speed: ${this.speed.toFixed(1)} m/s`, 10, 20);
    ctx.fillText(`Gamma: ${this.gamma.toFixed(3)}`, 10, 40);
}
```

#### 2. Coupled/Decoupled режимы

```javascript
// js/simulation/assist-system.js
updateCoupledMode() {
    if (this.coupled) {
        this.alignNoseToVelocity();
    }
}
```

#### 3. Мобильное управление (упрощенное)

```javascript
// js/input/touch.js
setupTouchControls() {
    // Простые кнопки вместо стиков сначала
}
```

### 🌌 **Фаза 5: Окружение и коллизии (1 неделя)**

#### 1. Генерация астероидов

```javascript
// js/world/asteroid-generator.js
generateAsteroids() {
    // Простые круги сначала
}
```

#### 2. AABB коллизии (простая реализация)

```javascript
// js/collision/aabb.js
checkAABBCollision(rect1, rect2) {
    return rect1.x < rect2.x + rect2.width &&
           rect1.x + rect1.width > rect2.x &&
           // ...
}
```

#### 3. Тороидальный мир

```javascript
// js/world/wrap-around.js
applyWrapAround() {
    if (this.x > WORLD_WIDTH) this.x = 0;
    if (this.x < 0) this.x = WORLD_WIDTH;
    // ...
}
```

### 🔧 **Фаза 6: Полировка и фичи (2-3 недели)**

#### 1. Альфа-коллизии

```javascript
// js/collision/alpha-mask.js
checkAlphaCollision(sprite1, sprite2) {
    // Более сложная проверка прозрачности
}
```

#### 2. G-лимиты и профили

```javascript
// js/simulation/g-limits.js
applyGLimits(requestedAcceleration) {
    return Math.min(requestedAcceleration, this.currentGLimit);
}
```

#### 3. Автопилот Random

```javascript
// js/autopilot/random-pilot.js
generateRandomCommand() {
    // Простой случайный выбор действий
}
```

### 🚀 **Фаза 7: Оптимизация и тестирование (1 неделя)**

#### 1. Профилирование производительности

```javascript
// js/utils/performance.js
measureFPS() {
    // Мониторинг FPS
}
```

#### 2. Тестирование по ТЗ

```markdown
- FF1: Коллизии ✓
- FF2: Coupled режим ✓  
- FF3: Decoupled режим ✓
- ...
```

## 💡 **Советы по работе с Copilot**

### Эффективные промпты:

```javascript
// Вместо: "Сделай физику"
// Лучше: "Реализуй обновление позиции корабля на основе velocity и deltaTime"

// Вместо: "Создай HUD"
// Лучше: "Нарисуй панель с показателями скорости, гамма-фактора и режима coupled в правом верхнем углу"
```

### Структура комментариев для Copilot:

```javascript
/**
 * Рассчитывает релятивистский гамма-фактор
 * @param {number} speed - текущая скорость в м/с
 * @param {number} cPrime - скорость "медленного света" (1000 м/с)
 * @returns {number} gamma factor
 */
function calculateGamma(speed, cPrime) {
    // Copilot предложит реализацию
}
```

## 🛠️ **Ежедневный workflow**

### Утро (30 мин):

```markdown
1. Прочитать следующий этап плана
2. Сформулировать конкретные задачи для Copilot
3. Написать заглушки функций с комментариями
```

### Работа (2-3 часа):

```markdown
1. Реализовать 1-2 небольшие функции с помощью Copilot
2. Протестировать в браузере
3. Закоммитить работающую версию
```

### Вечер (30 мин):

```markdown
1. Зафиксировать прогресс
2. Подготовить задачи на следующий день
3. Изучить непонятные концепции
```

## 🎯 **Критерии успеха каждого этапа**

- **Фаза 1**: Квадратик двигается по экрану от клавиш
- **Фаза 2**: Квадрат поворачивается и имеет инерцию
- **Фаза 3**: Добавлены ограничения скорости и ускорения
- **Фаза 4**: Есть HUD и два режима управления
- **Фаза 5**: Есть астероиды и столкновения
- **Фаза 6**: Все фичи ТЗ реализованы
- **Фаза 7**: Стабильно 60 FPS на тестовом стенде

Хотите, чтобы я детализировал какую-то конкретную фазу? Или начать с пошагового руководства по настройке среды?


