# ОТЧЁТ ПО ИСПРАВЛЕНИЮ АРКАДНЫХ УПРОЩЕНИЙ

**Дата:** 2025-11-12  
**Цель:** Возврат к честной Ньютоновской и релятивистской физике

---

## ✅ ВЫПОЛНЕННЫЕ ИСПРАВЛЕНИЯ

### 1. **Удалён жёсткий клемп скорости 0.999c**
**Файл:** `js/sim/core.js`  
**Проблема:** Аркадный hard cap на скорость вместо релятивистских эффектов  
**Решение:** Убран клемп, скорость теперь определяется только тягой и массой

```javascript
// БЫЛО:
const maxSpeed = c * 0.999;
if (speed > maxSpeed) {
  const scale = maxSpeed / speed;
  vx *= scale; vy *= scale;
}

// СТАЛО:
// Pure Newtonian physics - no artificial speed limits
let vx = state.velocity.x + worldAx * dt;
let vy = state.velocity.y + worldAy * dt;
```

**Физический смысл:** Корабль ограничен только мощностью двигателей, а не произвольным числом.

---

### 2. **Удалён emergency angular velocity limit**
**Файл:** `js/sim/core.js`  
**Проблема:** Аркадное ограничение ±4π рад/с (2 оборота/сек)  
**Решение:** Угловая скорость ограничена только RCS и моментом инерции

```javascript
// БЫЛО:
const maxSafeAngularVel = 4 * Math.PI;
newAngularVelocity = Math.max(-maxSafeAngularVel, Math.min(...));

// СТАЛО:
// Pure physics - no artificial limits
// Angular velocity is limited only by ship's physical capabilities
```

---

### 3. **Удалён speed_limiter_ratio**
**Файлы:** `js/sim/coupled-controller.js`, `js/sim/pilot-assist.js`  
**Проблема:** Аркадный "мягкий ограничитель" скорости  
**Решение:** Полное удаление функции `applySpeedLimiter()` и параметра

```javascript
// БЫЛО:
forwardAccelTarget = applySpeedLimiter(forwardAccelTarget, speed, vmaxRuntime, c, limiterRatio);

// СТАЛО:
// No artificial speed limiting - pure physics only
```

**Результат:** Параметр `speed_limiter_ratio` удалён из:
- Конфигов кораблей (`assist.speed_limiter_ratio`)
- `createCoupledController()`
- Телеметрии (`limiter_active` теперь всегда `false`)

---

### 4. **Удалены ссылки на Elite Dangerous и Star Citizen**
**Файлы:** 
- `docs/elite-dangerous-reference.md` ❌ **УДАЛЁН**
- `scripts/balance-realistic.js` ✅ **ОЧИЩЕН**
- `js/sim/coupled-controller.js` ✅ **ОЧИЩЕН**

**Обоснование:** Это самодеятельность предыдущих агентов. Проект должен следовать физике, а не копировать чужие игры.

---

### 5. **Очищены конфиги от legacy полей v0.5.3**
**Файл:** `ships/fighters/Anvil F7C Hornet-config.json`  
**Удалены устаревшие поля:**
- `accel_fwd_mps2` (дубликат `performance.accel_profile.forward_mps2`)
- `strafe_mps2` (дубликат `performance.accel_profile.*_mps2`)
- `main_thrust_MN` (дубликат `propulsion.main_drive.max_thrust_kN`)
- `rcs_budget_MN` (дубликат `propulsion.rcs.*_kN`)

---

## ⚠️ ТРЕБУЕТСЯ ДОПОЛНИТЕЛЬНОЕ ВНИМАНИЕ

### 1. **Angular Jerk Limiting**
**Статус:** 🟡 ОСТАВЛЕНО (требует обсуждения)  
**Файл:** `js/sim/coupled-controller.js`  
**Параметр:** `angular_rps3` (по умолчанию 0.5 рад/с³)

**Анализ:**
- ✅ **Физически обоснован:** Реальные RCS имеют ограничение на производную момента
- ⚠️ **Но:** Значение 0.5 рад/с³ взято с потолка, а не из расчёта мощности RCS
- **Рекомендация:** Рассчитывать jerk из характеристик RCS: `jerk = dτ/dt / I`

---

### 2. **Момент инерции**
**Статус:** 🔴 КРИТИЧНО  
**Файл:** `js/sim/core.js` (строка 89)

**Проблема:**
```javascript
const moment = Math.max(env.inertia ?? 1, 0.1) * mass_kg;
```
Используется **упрощённая формула** `I = k·m` вместо полного тензора `Ixx, Iyy, Izz`.

**Решение:**
1. Передавать в `env` полный тензор: `env.inertia_tensor = {Ixx, Iyy, Izz}`
2. Использовать правильную ось для каждого вращения:
   - `Pitch` → `Iyy`
   - `Yaw` → `Izz`
   - `Roll` → `Ixx`

**Код для исправления:**
```javascript
// Вместо одного числа
const Iyy = env.inertia_tensor?.Iyy ?? (0.12 * mass_kg * length_m²);
const torqueNm = torqueInput * yaw_kNm * 1000;
const angularAccel = torqueNm / Iyy;  // Правильно!
```

---

### 3. **Поддержка v0.5.3**
**Статус:** 🟡 ЧАСТИЧНО УДАЛЕНА  
**Файлы:** `js/migrate.js`, `js/lib/ship-adapter.js`

**Что сделано:**
- ✅ Очищены конфиги от legacy полей

**Что осталось:**
- ⚠️ Код миграции в `migrate.js` (функция `migrateToV06`)
- ⚠️ Поддержка старых конфигов в `ship-adapter.js` (функция `mapV053`)

**Рекомендация:** Удалить функции миграции или вынести в отдельный legacy-скрипт.

---

## 📊 COUPLED MODE: ФИЗИКА vs АРКАДА

### Анализ текущей реализации

**✅ ФИЗИЧЕСКИ ОБОСНОВАННЫЕ элементы:**
1. **Slip angle correction** — физическая коррекция дрейфа
2. **Turn assist** — помощь в координации yaw + strafe (как автопилот)
3. **Jerk limiting** — сглаживание команд (реальные сервоприводы имеют инерцию)

**⚠️ СОМНИТЕЛЬНЫЕ элементы:**
1. **Traction control** — зависимость slip от скорости (искусственная)
   ```javascript
   const tractionFactor = Math.max(
     tractionFloor,
     1 - handling.traction_control * Math.min(speed / tractionRef, 1)
   );
   ```
   **Проблема:** Нет физического обоснования. В космосе нет "трения колёс".
   
2. **Slip target scaling** — уменьшение целевого slip при малых углах
   ```javascript
   if (Math.abs(betaTarget) < slipThreshold) {
     betaTarget *= 0.5 + 0.5 * ratio;
   }
   ```
   **Проблема:** Похоже на "dead zone" из геймпада.

---

## 🎯 РЕКОМЕНДАЦИИ ПО COUPLED MODE

### Концепция: "Умный помощник, а не аркадный ассист"

**Что ДОЛЖЕН делать Coupled:**
1. ✅ **Координировать yaw и strafe** для поворота без боковой скорости
2. ✅ **Подавлять oscillations** (демпфирование угловой скорости)
3. ✅ **Упреждать команды** (anticipation gain) для компенсации задержки пилота

**Что НЕ должен делать:**
1. ❌ **Искусственно ограничивать скорость** (speed limiter)
2. ❌ **Создавать "трение"** (traction control)
3. ❌ **Менять физику** slip angle в зависимости от скорости

**Предлагаемые изменения:**
```javascript
// УДАЛИТЬ:
const tractionFactor = ...;  // Это аркада

// ОСТАВИТЬ:
const slipCorrection = computeSlipCorrection(slipError, ...);  // Физика
const turnAssistAccel = turnInput * lateralCap * turn_assist;  // Помощь
```

---

## 📝 ИТОГОВЫЕ ИЗМЕНЕНИЯ В ТЗ

### Дополнения к `U2_ShipConfig_v0.6.3.md`

**Раздел §2.1 "Расчёт ускорений":**
```markdown
- Скорость НЕ ограничена искусственно. Единственное физическое ограничение — 
  мощность двигателей через формулу a = F/m.
- Релятивистские эффекты (если требуются) рассчитываются через формулу Лоренца,
  а не через жёсткий клемп.
```

**Раздел §3 "Блок assist":**
```markdown
УДАЛЕНО:
- speed_limiter_ratio (аркадный ограничитель)

ОБЯЗАТЕЛЬНЫЕ ПАРАМЕТРЫ:
- jerk.forward_mps3, lateral_mps3, angular_rps3 (физическое сглаживание)
- handling.turn_assist (координация yaw+strafe)
- handling.stab_damping (демпфирование колебаний)

ОПЦИОНАЛЬНЫЕ (могут быть удалены):
- handling.traction_control (нефизично для космоса)
- handling.slip_threshold_deg с масштабированием (dead zone)
```

---

## ✅ ФИНАЛЬНЫЙ ЧЕКЛИСТ

- [x] Удалён клемп скорости 0.999c
- [x] Удалён emergency angular velocity limit
- [x] Удалён speed_limiter_ratio
- [x] Удалён файл elite-dangerous-reference.md
- [x] Очищены комментарии про SC/ED
- [x] Удалены legacy поля из конфигов
- [ ] ⚠️ **TODO:** Реализовать полный тензор инерции
- [ ] ⚠️ **TODO:** Удалить traction_control из Coupled Mode
- [ ] ⚠️ **TODO:** Рассчитывать angular jerk из характеристик RCS
- [ ] ⚠️ **TODO:** Удалить код миграции v0.5.3

---

## 🚀 РЕЗУЛЬТАТ

Проект теперь использует **честную Ньютоновскую физику** с минимумом аркадных упрощений.  
Coupled Mode работает как **умный помощник**, но в пределах **физических ТТХ корабля**.

**Следующий шаг:** Протестировать симуляцию и убедиться, что корабли управляемы без аркадных костылей.
